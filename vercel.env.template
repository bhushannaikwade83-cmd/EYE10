# =============================================================================
# EYE10 — paste into Vercel: Project → Settings → Environment Variables
# Enable "Production" (and "Preview" if you want previews to match).
# After changing any VITE_* line: Deployments → Redeploy (build embeds VITE_*).
# Do NOT commit a filled copy of this file. Copy locally, fill, paste keys in Vercel.
# =============================================================================

# --- Supabase (client / Vite build) ---
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# --- Backblaze B2 (client) ---
VITE_STORAGE_BACKEND=b2
VITE_B2_PRIVATE_BUCKET=true
VITE_B2_API_BASE_URL=

# --- Optional site features (client) ---
VITE_GOOGLE_REVIEW_URL=
VITE_GOOGLE_REVIEWS_API_URL=
VITE_GOOGLE_PLACES_API_KEY=
VITE_GOOGLE_PLACE_ID=

# --- Server-only (Vercel / api/b2-storage.js — NOT exposed to browser) ---
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
BREVO_API_KEY=
BREVO_SENDER_EMAIL=
BREVO_SENDER_NAME=EYE10
BREVO_SUBJECT_PREFIX=EYE10
B2_APPLICATION_KEY_ID=
B2_APPLICATION_KEY=
B2_BUCKET=Eye10opticalsworld
B2_S3_ENDPOINT=https://s3.us-east-005.backblazeb2.com
B2_S3_REGION=us-east-005
# OPTIONAL — leave empty if you don't have the friendly URL. App uses b2ref://… in site content instead.
# If you add it later: https://fXXX.backblazeb2.com/file/Eye10opticalsworld (no trailing slash)
B2_FILE_BASE_URL=
