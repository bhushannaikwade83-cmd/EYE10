# Push to GitHub - bhushannaikwade83/eye10

Your code is committed locally. To push it to GitHub:

## Step 1: Create the repository on GitHub

1. Open: **https://github.com/new**
2. **Repository name:** `eye10`
3. **Description (optional):** EYE10 eyewear website
4. Choose **Public**
5. Do **NOT** add a README, .gitignore, or license (we already have these)
6. Click **Create repository**

## Step 2: Push your code

In terminal (PowerShell or Command Prompt), run:

```bash
cd c:\Users\LUZARDO\Desktop\EYE10
git push -u origin main
```

If GitHub asks for login, use your GitHub username **bhushannaikwade83** and either:
- Your password, or
- A **Personal Access Token** (if you use 2FA: GitHub → Settings → Developer settings → Personal access tokens).

---

**Done locally:**
- Git repo initialized
- `.gitignore` added (node_modules, dist, .env excluded)
- All project files committed
- Remote set to: `https://github.com/bhushannaikwade83/eye10.git`
- Branch renamed to `main`

After you create the repo on GitHub, the push command above will upload everything.
